# Inline tables must be edited without reformatting the outer document.
projects = { {{PROJECT_KEY}} = { keep = "yes", nested = { value = 1 }, trust_level = "trusted" }, "other" = { trust_level = "trusted" } }
unrelated = "keep this value"
