# Dotted project keys are semantically one project table.
projects.{{PROJECT_KEY}}.name = "backslash path" # retain the comment
projects.{{PROJECT_KEY}}.nested = [
  "one",
  "two",
]

[other]
enabled = true
